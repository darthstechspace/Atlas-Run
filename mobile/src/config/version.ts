/** App release label; keep in sync with package.json and app.config.ts */
export const APP_VERSION = '0.5.0-beta';
export const APP_VERSION_SHORT = 'v0.5 beta';

/** Native build counter; iOS buildNumber and Android versionCode must stay equal. */
export const NATIVE_BUILD_VERSION = 1;
